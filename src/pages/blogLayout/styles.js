import { makeStyles } from "@mui/styles";

export default makeStyles(theme => ({
  contentsArea: {
    position: "fixed",
    left: "240px",
    top: "50px",
    padding: "10px 10px 10px 10px"
  },
  contents: {
    margin: "0px"
  }
}));
