---
title: CD 플레이어 페이지 만들기 
categories: 
  - interactive_page
tags:
  - interactive_page
  
toc: true
---

기존에 공부한
1. 스크롤 기능을 이용한 페이지 전환
2. 점4 기능을 이용한 스크롤 이동

을 이용해서 새로운 인터렉티브 페이지인 CD-P 페이지를 만들어보자.

## 1. 알아두기

### SCSS
- 기존의 CSS를 더 쉽게 개발할 수 있게 해주는 개념. 정확한 뜻은 아님
- SCSS 을 컴파일하여 CSS로 변환.
- CSS 에서는 할 수 없는 TREE 단계 구조로 선언이 가능하여 HTML 소스의 구조와 비슷하게 개발 가능.
- 동일한 내용이 여러번 들어갈 것을 줄여주고, 가독성이 좋아짐.

### reset.css
- CSS 를 하기전에 CSS 의 기본 틀을 만들어주는 소스 . 틀을 잡아줌

### CSS 기능

## 2. 주요기능
### 페이지 전환 스크립트
```javascript
window.onload = function(){
    prev_button = document.querySelectorAll("button")[0];
    next_button = document.querySelectorAll("button")[1];
    
    totalNum = album.length;
    // alert(totalNum)

    prev_button.addEventListener("click", function(){
        if(pageNum > 0){
            pageNum --;
        }else{
            pageNum = totalNum -1;
        }
        pageChangeFunc();
    })

    next_button.addEventListener("click", function(){
        if(pageNum < totalNum-1){
            pageNum ++;
        }else{
            pageNum = 0;
        }
        pageChangeFunc();
    })

    //최초실행
    // pageNum = 2;
    pageChangeFunc();
}

//여기서 모든 것을 한다.
function pageChangeFunc(){

    for(var i=0; i<totalNum; i++){
        if(pageNum == i){
            //현재 컨텐츠(페이지)
            album[i].classList.add("active");
        }else{
            album[i].classList.remove("active");
        }
    }
}
```

## 3. 시작하기
### 기본 템플릿 추가
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CD 플레이어</title>
    <style>
    </style>
    <script>
    </script>
    </head>
    <body>

    </body>
</html>
```
아무것도 없는 빈화면 이 뜬다.

### 점4


```html
     <ul class="pointWrap">
        <li></li>
        <li></li>
        <li></li>
        <li></li>
      </ul>
```

[![기본소스만들기](/assets/imgs/interactive-cdplayer/1.init.png)](/assets/imgs/imgs/interactive-cdplayer/1.init.png)


### reset.css 추가
이번에 새로 배운 reset.css 을 사용해보자.
인터넷에 돌아다니는 소스를 그냥 쓰면 된다.

기본에 작성하였던 ```<style>``` 태그 위에 아래코드를 추가하자. 해당경로에 ```reset.css```를 넣는 것도 잊지말자.

```html
<link rel="stylesheet" type="text/css" href="./css/reset.css" />
```

[![reset 추가](/assets/imgs/interactive-cdplayer/2.reset.png)](/assets/imgs/imgs/interactive-cdplayer/2.reset.png)

> 보이는 바와같이 별반 차이가 없다

### 내용채우기
- 내용 : 
  - 앨범사진 + CD(움직이는)
  - 버튼 PREV, NEXT -> 클릭시 앨범사진+CD 가 변경됨(페이지 전환)

앨범사진 + CD
```html
<article>

    <section>
      <div>
        <div></div>
      </div>
      <a href="#" target="blank" >
        <img src="" alt="">
      </a>
    </section>

    <section>
        <div>
          <div></div>
        </div>
        <a href="#" target="blank" >
          <img src="" alt="">
        </a>
      </section>

      <section>
        <div>
          <div></div>
        </div>
        <a href="#" target="blank" >
          <img src="" alt="">
        </a>
      </section>

  </article>
```html

버튼 PREV, NEXT
```html
<div>
<button type="button">PREV</button>
<ul class="pointWrap">
  <li></li>
  <li></li>
  <li></li>
</ul>
<button type="button">NEXT</button>
</div>
```


[![content 추가](/assets/imgs/interactive-cdplayer/3.content.png)](/assets/imgs/imgs/interactive-cdplayer/3.content.png)


> 전체를 DIV 로 감싸고, 기존 ul 위아래로 button 을 추가한다.
> 점4 버튼 위아래로 PREV, NEXT 버튼이 추가 된 것을 볼 수 있다.


### 미리준비한 내용 더 채우기

- a 태그 url
- img url

```html
<section>
  <div>
    <div></div>
  </div>
  <a href="https://www.youtube.com/watch?v=nM0xDI5R50E" target="blank" >
    <img src="./image/iu_0.jpg" alt="">
  </a>
</section>

<section>
    <div>
      <div></div>
    </div>
    <a href="https://www.youtube.com/watch?v=Ub6IyAOTwr8" target="blank" >
      <img src="./image/iu_1.jpg" alt="">
    </a>
  </section>

  <section>
    <div>
      <div></div>
    </div>
    <a href="https://www.youtube.com/watch?v=D1PvIWdJ8xo" target="blank" >
      <img src="./image/iu_2.jpg" alt="">
    </a>
  </section>
```

[![content2 추가](/assets/imgs/interactive-cdplayer/4.content2.gif)](/assets/imgs/imgs/interactive-cdplayer/4.content2.gif)

> 서버작업없이 공부 중 이기 때문에 일일이 퍼오자
> 이미지를 찾고, 이미지와 연관된 youtube 링크를 복사하자.
> 작업하고나니 이미지가 너무 크다. 내가 원하는건 화면 가운데에 앨범과 CD가 예쁘게 있고, 점4가 화면 아래에 가로로 정렬되는 모습이다.

### SCSS 개념을 이용한 화면 UI 수정
- SCSS 파일 작성 및 HTML 태그에 링크 추가

```html
<link rel="stylesheet" type="text/css" href="./css/motion.css" />
```

1. 전체영역 & 폰트사이즈

```css
$mobile: "only screen and (max-width: 720px)";
$t_size : 16px;

body {
    overflow: hidden;
    font-size: $t_size;
    
}
```

> 폰트와 스크롤 기능을 없앴다.

2. 내용 CSS 구조 입히기


```html
  <article class="contentWrap">

    <section class="album active">
      <div class="disk">
        <div class="disk_inner"></div>
      </div>
      <a href="https://www.youtube.com/watch?v=nM0xDI5R50E" target="blank" class="coverImg">
        <img src="./image/iu_0.jpg" alt="">
      </a>
    </section>

    <section class="album">
      <div class="disk">
        <div class="disk_inner"></div>
      </div>
      <a href="https://www.youtube.com/watch?v=Ub6IyAOTwr8"  target="blank" class="coverImg">
        <img src="./image/iu_1.jpg" alt="">
      </a>
    </section>

    <section class="album ">
      <div class="disk">
        <div class="disk_inner"></div>
      </div>
      <a href="https://www.youtube.com/watch?v=D1PvIWdJ8xo"  target="blank" class="coverImg">
        <img src="./image/iu_2.jpg" alt="">
      </a>
    </section>

  </article>

```

> 기존의 a 링크와 img 만 넣었지만, 이제는 CSS 적용을 위해 각 태그에 클래스명을 입력하자.

[![class 추가](/assets/imgs/interactive-cdplayer/5.nonscroll.png)](/assets/imgs/imgs/interactive-cdplayer/5.nonscroll.png)

> 위에 사진은 크롬화면에 보이는 전부이다.

#### 앨범

- article : ```.contenWrap``` 배경과 가로세로 꽉채우는 css를 적용
- section : ```.album``` 앨범의 위치가 화면에 정중앙에 오도록 CSS를 입히자. ```.active``` 활성화된 앨범만 보일 수 있게 ```opacity```, ```visibility``` 를 조절하자.
- a img : ```coverImg``` 이미지의 크기와 위치를 정한다.

> 결국 배경과 앨범이미지의 위치를 정중앙으로 배치한다는 말.

```css


.contentWrap {
    background: linear-gradient(120deg, #0272a4, #f6a564);
    // background: linear-gradient(125deg, #b6bfc8, #36595b);
    // background:linear-gradient(125deg, #e58e82, #6f569f);
    transition: background 1s ease-out;  
    width:100vw;
    height: 100vh;
    touch-action: none;


    .album {
        position: absolute;
        top: 47%;
        left: 55%;
        transform: translate(-50%, -50%);
        width: 600px;
        height: 400px;
        visibility: hidden;
        opacity: 0;

        &.active {
            visibility: visible;
            opacity: 1;
            left: 50%;
        }
        .coverImg {
            position: absolute;
            width: 400px;
            height: 400px;
            img {
                width: inherit;
                height: inherit;
            }
        }
    }
     
}
```

[![img css](/assets/imgs/interactive-cdplayer/6.imgcss.png)](/assets/imgs/imgs/interactive-cdplayer/6.imgcss.png)

#### 점4

- li 의 모양을 원형으로 만들고. 클릭시 또는 페이지 전환시 활성화 표시되게 한다.
- PREV, 점4, NEXT 순으로 배치를 예쁘게한다.

```css

.buttonWrap {
    position : fixed;
    bottom : 5vh;
    text-align: center;
    width: 100%;

    button {
        padding : 6px 20px;
        margin: 0 3px;
        background-color: #000;
        color: #fff;
        transition: all .3s ease-out;

        &:hover {
            background-color: #fff;
            color : #000;
        }
    }

    .pointWrap {
        position: relative;
        display: inline-block;
        padding: 0 10px;

        li {
            position : relative;
            display: inline-block;
            width: 10px;
            height: 10px;
            margin: 0 5px;
            text-align: center;
            cursor : pointer;
            background-color: rgba(0,0,0,.4);
            border-radius: 50%;
            transition : background .3s ease-out;

            &.active {
                background-color: rgba(0,0,0,1);
            }
        }
    }
}
```


[![btn css](/assets/imgs/interactive-cdplayer/7.btncss.png)](/assets/imgs/imgs/interactive-cdplayer/7.btncss.png)

> 제대로된 설명은 없지만 화면이 예뻐졌다.

#### CD 배치

- CD 는 CSS 로 만듬
- CD 는 플레이효과를 주기위해 뺑뺑 도는 효과를 추가.

```css
        .disk {
            position: absolute;
            top: 4px;
            left : 0px;
            width: 392px;
            height: 392px;
            border-radius: 50%;
            // background-color: black;
            background: linear-gradient(120deg, #000, #333333, #000);
            transition : left .6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            animation: rotateAni 10s ease infinite;
            // transition-delay: 0s;

        }
```

.coverImg .active 안의 .disk 를 추가하여 디스크가 보이게 하자.
```css
        &.active {
            ...
            .disk {
                left : 180px;
                transition-delay: .5s;
            }
        }
```

> div.disk 의 위치를 잡기 위해 position, 상하좌우 크기를 지정
> 디스크모양(원형) 을 만들기 위해 border-radius:50%;
> 디스크 광원 효과를 주기 위해 background: linear-gradient(120deg, #000, #333333, #000);



앨범과 디스크에 그림자효과를 주어 꾸미자

앨범

```css
.coverImg{
  ...
  box-shadow: 2px 14px 40px rgba(0,0,0,.3);
}
```

디스크

```css
.disk{
  box-shadow: 4px 14px 40px rgba(0,0,0,.3);
}
```


[![shadow css](/assets/imgs/interactive-cdplayer/8.shadow.png)](/assets/imgs/imgs/interactive-cdplayer/8.shadow.png)


> 그럴싸해 졌다.

#### CD 꾸미기

동일한 형식으로 .disk 안에 아래 코드를 추가하자.

```css

            .disk_inner {
                position: absolute;
                top:50%;
                left: 50%;
                transform: translate(-50%, -50%);
                width : 160px;
                height: 160px;
                background-color: #0473a4;
                border-radius: 50%;
                border : 3px dashed rgba(255,255,255,.4);
            }
```

> 위치 중앙에. 크기 지정. 색상. 원형. 테두리 꾸미기 등에 대한 CSS이다.


### 페이지 전환 - 앨범
기존에 공부한 페이지 전환 기능을 이용하여 
PREV, NEXT 클릭시 점4가 이동하며, 앨범과 CD가 변경 되는 것을 해보자.

```javascript
var prev_button, next_button;
var contentWrap;
var disk_inner;
var pageNum = 0;
var totalNum = 0;
var album;
var pointBtnAll;
var bgArray = new Array();
bgArray[0] = ["#0272a4","#f6a564"];
bgArray[1] = ["#b6bfc8","#36595b"];
bgArray[2] = ["#e58e82","#6f569f"];

window.onload = function(){
    prev_button = document.querySelectorAll("button")[0];
    next_button = document.querySelectorAll("button")[1];
    
    contentWrap = document.querySelector(".contentWrap");
    disk_inner = document.querySelectorAll(".disk_inner");
    album = document.querySelectorAll(".album");
    totalNum = album.length;
    // alert(totalNum)

    prev_button.addEventListener("click", function(){
        if(pageNum > 0){
            pageNum --;
        }else{
            pageNum = totalNum -1;
        }
        pageChangeFunc();
    })

    next_button.addEventListener("click", function(){
        if(pageNum < totalNum-1){
            pageNum ++;
        }else{
            pageNum = 0;
        }
        pageChangeFunc();
    })

    //최초실행
    // pageNum = 2;
    pageChangeFunc();
}

//여기서 모든 것을 한다.
function pageChangeFunc(){

    // console.log(bgArray[pageNum][1])
    contentWrap.style.background = "linear-gradient(120deg,"+ bgArray[pageNum][0] +", "+ bgArray[pageNum][1] + ")";

    for(var i=0; i<totalNum; i++){
        if(pageNum == i){
            //현재 컨텐츠(페이지)
            album[i].classList.add("active");
        }else{
            album[i].classList.remove("active");
        }
    }
    
    // album[pageNum].classList.add("active");

    disk_inner[pageNum].style.background = bgArray[pageNum][0];
}
```

기존에 알던 것 과 다른 부분은 
1. 변수들 전역변수화
2. contentWrap, album, disk 를 가져와 페이지 전환시 Active 클래스를 밀어넣고(CSS 전환효과 기대)
3. 페이지 전환 동시에 disk 내부색과 화면 배경색을 미리 지정한 배열로 변경.

[![페이지 전환](/assets/imgs/interactive-cdplayer/9.pageconvert.gif)](/assets/imgs/imgs/interactive-cdplayer/9.pageconvert.gif)


### 페이지 전환 - 점4

```javascript
...
var pointBtnAll;
...

pointBtnAll = document.querySelectorAll(".pointWrap li");
...
for( var i = 0; i < pointBtnAll.length; i++ ){
    (function(idx) {
        pointBtnAll[idx].onclick = function() {
            // alert(idx);
            pageNum = idx;
            pageChangeFunc();
        }
    })(i);
}
...
  for(var i=0; i<totalNum; i++){
      if(pageNum == i){
          //현재 컨텐츠(페이지)
          album[i].classList.add("active");
          pointBtnAll[i].classList.add("active");
      }else{
          album[i].classList.remove("active");
          pointBtnAll[i].classList.remove("active");
      }
  }
```

엘범을 전환하고 나니 페이지 표시를 할 점4의 위치가 표시 되지 않는다.
위 스크립트를 추가하여 페이지 위치가 표시되도록 수정하자.


[![페이지 전환](/assets/imgs/interactive-cdplayer/10.pageconvert2.gif)](/assets/imgs/imgs/interactive-cdplayer/10.pageconvert2.gif)